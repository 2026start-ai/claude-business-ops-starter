# 🔨 Fix-it — defects noticed in passing

Bugs and debt spotted while working on something else. Quick + low-risk
gets fixed on the spot instead of landing here; everything else is
logged in the moment and knocked out when a session's work brushes it.
Weighty items graduate to `backlog.md`.

<!-- Format (real items start at column 0; this example is indented so
     the session-start hook doesn't surface it):
  - [ ] <defect> — <where> — added <YYYY-MM-DD>
-->
